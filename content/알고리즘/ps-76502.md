---
title: "[스택] 76502: 괄호 회전하기"
slug: ps-76502
category: 알고리즘
tags:
  - 스택
  - 문자열처리
  - 구현
date: 2026-07-09
published: 2026-07-09
draft: false
description: ""
created: 2026-07-09 14:05
modified: 2026-07-09 14:06
---
문제:
![[Pasted image 20260709140636.png]]https://school.programmers.co.kr/learn/courses/30/lessons/76502

풀이:
```java
import java.util.*;

class Solution {
    public int solution(String s) {
        int answer = 0;

        for (int start = 0; start < s.length(); start++) {
            if (check(s, start)) answer++;
        }

        return answer;
    }

    private boolean check(String s, int start) {
        Stack<Character> stack = new Stack<>();
        int len = s.length();

        for (int i = 0; i < len; i++) {
            char c = s.charAt((start + i) % len);

            if (c == '(' || c == '{' || c == '[') {
                stack.push(c);
            } else {
                if (stack.isEmpty()) return false;

                char open = stack.peek();
                if (!isPair(open, c)) return false;

                stack.pop();
            }
        }

        return stack.isEmpty();
    }

    private boolean isPair(char open, char close) {
        return (open == '(' && close == ')')
            || (open == '{' && close == '}')
            || (open == '[' && close == ']');
    }
}
```
