---
title: "[문자열 처리] 64065: 튜플"
slug: ps-64065
category: 알고리즘
tags:
  - 문자열처리
  - 정렬
  - 해시
date: 2026-07-15
published: 2026-07-15
draft: false
description: ""
created: 2026-07-15 23:22
modified: 2026-07-15 23:24
---
문제:
![[Pasted image 20260715232246.png]]
https://school.programmers.co.kr/learn/courses/30/lessons/64065

풀이:
`Set.add(n)`은 새로운 숫자를 추가하면 `true`, 이미 있으면 `false`를 반환함!
`Arrays.sort()`에 정렬 기준을 직접 전달해서 깔끔하게 쓰기

```java
import java.util.*;

class Solution {
    public int[] solution(String s) {
        String str = s.substring(2, s.length()-2);
        String[] arr = str.split("\\},\\{");
        Arrays.sort(arr, (a, b) -> a.length()-b.length());
        
        int[] ans = new int[arr.length];
        HashSet<Integer> set = new HashSet<>();
        
        for(int i=0; i<arr.length; i++){
            String[] numbers = arr[i].split(",");
            for(String num : numbers){
                int n = Integer.parseInt(num);
                if(set.add(n)){
                    ans[i] = n;
                }
            }
        }
        
        return ans;
    }
}
```
