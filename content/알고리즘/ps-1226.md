---
title: "[BFS] 1226: 미로1"
slug: ps-1226
category: 알고리즘
tags:
  - BFS
date: 2026-09-03
draft: false
description: ""
created: 2026-09-14 16:25
modified: 2026-09-14 16:25
published: 2026-09-03
---
문제:
![[Pasted image 20260914162540.png]]

풀이:
```java
import java.io.*;
import java.util.*;

class Solution {
	static int[][] arr;
	static int sx, sy, fx, fy;
	static int[] dx = {-1, 1, 0, 0}, dy = {0, 0, -1, 1};
	
	public static void main(String[] args) throws Exception {
		// 로컬 테스트용: 같은 폴더의 input.txt를 입력으로 사용. 제출할 때 이 줄 주석 처리
		//System.setIn(Solution.class.getResourceAsStream("input.txt"));

//		Scanner sc = new Scanner(System.in);
//		int T = sc.nextInt();
		
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		int T = 10;
		
		for (int test_case = 1; test_case <= T; test_case++) {
			int num = Integer.parseInt(br.readLine());
			arr = new int[16][16];
			for(int i=0; i<16; i++) {
				String str = br.readLine();
				for(int j=0; j<16; j++) {
					arr[i][j] = str.charAt(j)-'0';
					if(arr[i][j] == 2) {sx = i; sy = j;}
					else if(arr[i][j] == 3) {fx = i; fy = j;}
				}
			}
			
			System.out.println("#" + num + " " + bfs());
		}
	}
	
	public static int bfs() {
		Queue<int[]> q = new LinkedList<>();
		boolean[][] visited = new boolean[arr.length][arr[0].length];
		q.offer(new int[] {sx, sy});
		visited[sx][sy] = true;
		
		while(!q.isEmpty()) {
			int[] cur = q.poll();
			int x = cur[0];
			int y = cur[1];
			if(x == fx && y == fy) return 1;
			
			for(int k=0; k<4; k++) {
				int nx = x+dx[k];
				int ny = y+dy[k];
				if(nx>=0 && ny>=0 && nx<arr.length && ny<arr[0].length) {
					if(!visited[nx][ny] && arr[nx][ny] != 1) {
						visited[nx][ny] = true;
						q.offer(new int[] {nx, ny});
					}
				}
			}
		}
		
		return 0;
	}
}

```
