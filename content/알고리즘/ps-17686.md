---
title: "[정렬] 17686: 파일명 정렬"
slug: ps-17686
category: 알고리즘
tags:
  - 정렬
  - 문자열파싱
  - 다중조건정렬
date: 2026-07-28
draft: false
description: ""
created: 2026-07-29 02:32
modified: 2026-07-29 02:34
---
문제:
![[Pasted image 20260729023341.png]]
https://school.programmers.co.kr/learn/courses/30/lessons/17686

풀이:

람다식 쓰기가 넘 어렵다..

```java
import java.io.*;
import java.util.*;

class Solution {
    public String[] solution(String[] files) {
        String[][] f = new String[files.length][3];
        
        for(int i=0; i<files.length; i++){
            String str = files[i];
            int j=0;
            int nb = 0, tb = str.length();
            while(j<str.length()){
                if(Character.isDigit(str.charAt(j))) { nb = j; break; }
                j++;
            }
            while(j<str.length()){
                if(!Character.isDigit(str.charAt(j))){ tb = j; break; }
                j++;
            }
            String head = str.substring(0, nb);
            String num = str.substring(nb, tb);
            
            f[i][0] = head; 
            f[i][1] = num;
            f[i][2] = str;
        }
        
        Arrays.sort(f, (a, b) -> {
            int headCompare = a[0].compareToIgnoreCase(b[0]);

            if (headCompare != 0) {
                return headCompare;
            }

            return Integer.compare(
                Integer.parseInt(a[1]),
                Integer.parseInt(b[1])
            );
        });
        
        String[] ans = new String[files.length];
        for(int i=0; i<ans.length; i++){
            ans[i] = f[i][2];
        }
        
        return ans;
    }
}
```
